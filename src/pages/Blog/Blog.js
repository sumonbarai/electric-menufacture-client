import React from "react";
import Spinner from "../../shared/Spinner/Spinner";

const Blog = () => {
  return (
    <div>
      <h1>this is blog page</h1>
      <Spinner></Spinner>
    </div>
  );
};

export default Blog;
